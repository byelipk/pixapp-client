<form id="upload-form" {{action "upload" on="submit"}} enctype="multipart/form-data" accept-charset="UTF-8">
  <input type="file" id="file" name="picture[image]" style="display:none" multiple onchange={{action "handleFiles"}}>
  {{!-- <input type="submit" name="submit" value="Upload"> --}}
</form>

{{!-- <ul id="file-list">
  {{#each files as |file|}}
    <li>
      <p>{{file.name}}</p>
      <img src={{file-url file}} alt={{file.name}} height="150" width="150"/>
      <p><a {{action "removeFile" file}} href="#">Remove</a></p>
    </li>
  {{else}}
    <p>
      You haven't selected any files!
    </p>
  {{/each}}
</ul> --}}
