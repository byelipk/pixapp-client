import { fileUrl } from 'pix-client/helpers/file-url';
import { module, test } from 'qunit';

module('Unit | Helper | file url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fileUrl([42]);
  assert.ok(result);
});
